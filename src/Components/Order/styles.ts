import styled from 'styled-components'

export const OrderContainer = styled.div`
  line-height: 22px;

  div {
    margin: 16px 0;
    flex-direction: column;
    display: flex;
    gap: 16px;
  }

  h2 {
    font-weight: bold;
    font-size: 16px;
  }

  p {
    font-size: 14px;
  }
`
